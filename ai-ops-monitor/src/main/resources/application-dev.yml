# 本地单机模式特有配置
monitor:
  mode: standalone

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/aiops_monitor?useUnicode=true&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    username: root
    password: root
  data:
    redis:
      host: localhost
      port: 6379